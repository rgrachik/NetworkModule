# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TestNetworkLayer' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TestNetworkLayer

workspace 'TestNetworkLayer.xcworkspace'

pod 'SnapKit'

target 'NetworkManager' do
    project 'Pods/NetworkManager/NetworkManager.xcodeproj'
  end

end
